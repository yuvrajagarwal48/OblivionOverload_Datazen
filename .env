# FinSim-MAPPO — Environment Configuration

# ═══ Supabase (Auth & Persistence) ═══
REACT_APP_SUPABASE_URL=https://eecxipoqwpkoiaqmzcql.supabase.co
REACT_APP_SUPABASE_ANON_KEY=sb_publishable_ampC5huV5qwa5_lSKFNccg_PAWO4D5e

# ═══ API Mode ═══
# true  = Run with mock data (no backend needed)
# false = Connect to FastAPI backend (default)
REACT_APP_USE_MOCK=false

# ═══ Backend URLs (only needed in production) ═══
# In development, CRA proxy (package.json "proxy") handles routing to localhost:8000
# Uncomment and set this when deploying to production:
REACT_APP_API_URL=http://127.0.0.1:8000
